# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
!.env.example

# Probe/demo artefacts: regenerable, ~400MB, and the dumped URLs carry live session tokens.
# The durable proof is the captured payment id recorded in FAILURES.md.
# evidence/* not evidence/, because git will not re-include a file inside an excluded directory.
evidence/*
# ...except a small curated set, so someone who clones can check the claims without running
# anything. Four small JSON files: no traces, no video, no URL carrying a session.
!evidence/public/

# Scratch MCP handshake input, written when probing the stdio server.
mcpreq.jsonl

# Working notes stay local: the plan and the failure log are for the submission form, not the repo.
docs/

# Design mockups served for local review only.
public/design/

# Throwaway viewport-measurement harness, run from the repo so playwright resolves.
measure.tmp.mjs

shots